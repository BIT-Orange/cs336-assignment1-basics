# Agent Operating Guide

本文件用于指导通用 AI coding agent 在本仓库中工作。目标是产出最有效、最优秀、最容易学习和面试讲解的代码。

## Mission

Agent 的任务不是只给提示，而是帮助我把需求变成高质量代码：

- 读懂现有代码、测试和项目配置。
- 给出完整实现、精确修改或可运行示例。
- 保证代码正确、简洁、可测试、可解释。
- 把最终结果组织成我可以快速学习和用于面试项目展示的形式。

## Default Workflow

处理实现类任务时，按以下流程执行：

1. 查看相关文件、测试、接口和项目配置。
2. 明确最小可行修改范围。
3. 实现一版清晰、可靠、符合项目风格的代码。
4. 运行相关测试或说明无法运行的原因。
5. 总结修改内容、验证结果和面试讲解点。

不要停留在空泛建议。如果需求足够明确，直接修改代码。

## What Excellent Code Means Here

优秀代码应满足：

- **通过测试**: 优先满足现有 `pytest` 行为和公开接口。
- **容易读**: 函数职责清晰，变量命名表达含义，控制流直接。
- **容易讲**: 能解释算法、shape、复杂度、边界情况和工程取舍。
- **不过度设计**: 不为小问题引入大框架或复杂层级。
- **性能合理**: 对 BPE、tokenizer、attention、optimizer、training loop 等核心路径避免明显低效实现。
- **可维护**: 和仓库已有风格一致，少做无关重构。

## Project Commands

常用命令：

```sh
uv run pytest
uv run pytest tests/test_model.py
uv run pytest tests/test_tokenizer.py
uv run pytest tests/test_train_bpe.py
uv run ruff check .
```

只改小范围代码时，优先运行对应测试文件；改动共享逻辑时再运行完整测试。

## Code Style

本项目使用 Python 3.12+，主要依赖 PyTorch、NumPy、jaxtyping、regex。

编码偏好：

- 保留函数签名和测试适配器约定。
- 使用类型标注和清晰 docstring，但不要写无意义注释。
- Tensor 代码中明确 batch、sequence、head、hidden 等维度含义。
- 避免把简单逻辑拆成过多层。
- 避免修改与当前任务无关的文件。
- 默认 ASCII；只有文档或已有文件需要中文时才使用中文。

## Output Standard

最终答复必须包含：

- 改了哪些文件。
- 关键实现思路。
- 运行了哪些验证命令及结果。
- 我可以如何在面试中解释这段实现。

如果没有运行测试，要明确说明原因。

## Learning Mode

当我说“用于学习”“用于面试项目”“给我最好的代码”“展示优秀实现”时，Agent 应：

- 给出完整、干净、可运行的实现。
- 解释为什么这是推荐实现。
- 指出常见错误和边界情况。
- 补充复杂度分析。
- 给出 3-5 个面试讲解点。

不要只说“你可以尝试”。应直接展示高质量代码。

## Review Mode

当我要求 review 时，先列问题，再给改法：

- 正确性 bug
- 边界情况
- 性能瓶颈
- 可读性和维护性
- 测试缺口

如果代码可以明显变好，应给出推荐实现或补丁。

## Academic and Integrity Boundary

这个仓库来源于课程作业。若用户明确表示代码会被直接作为课程提交，Agent 应提醒遵守课程规则，并转为解释、调试指导、测试建议和代码 review。

若用户说明目标是个人学习、复习、面试项目或参考实现，Agent 应提供完整高质量代码，并确保解释足够清楚，使用户能够真正理解和讲出来。

## Interview-Ready Explanation Checklist

每个核心实现完成后，尽量帮助我回答：

- 这段代码解决了什么问题？
- 输入输出和关键 shape 是什么？
- 为什么这个算法是正确的？
- 时间复杂度和空间复杂度是多少？
- 主要边界情况有哪些？
- 如果要扩展或优化，下一步会怎么做？

